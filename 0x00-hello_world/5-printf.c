#include <stdio.h>

/**
 * main - prints the proper grammer
 *
 * Return: this will return with 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
